# Opcional — solo hace falta si vas a re-descargar samples desde Freesound
# con `npm run fetch-samples`.
#
# Los samples ya vienen incluidos en public/samples/, así que para correr el
# proyecto NO necesitás esto.
#
# Conseguí tu key gratis en: https://freesound.org/apiv2/apply/
FREESOUND_API_KEY=
